{
  "name": "@autofiy/rac-material",
  "version": "1.1.0",
  "private": false,
  "dependencies": {
    "@autofiy/property": "^1.0.2",
    "@autofiy/rac-core": "^1.1.0",
    "react": "^16.13.1",
    "@material-ui/core": "^4.11.1"
  },
  "main": "build/main.js",
  "types": "build/main.d.ts",
  "devDependencies": {
    "@testing-library/jest-dom": "^5.11.4",
    "@testing-library/react": "^11.1.0",
    "@testing-library/user-event": "^12.1.10",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "react-dom": "^16.13.1",
    "react-scripts": "3.4.3",
    "typescript": "^4.1.2"
  },
  "scripts": {
    "start": "npm run serve & react-scripts start",
    "serve": "node ./test-server/server.js",
    "build": "tsc --noEmit false",
    "test": "react-scripts test",
    "deploy": "rm -rf build && npm run build && npm publish --access=public",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
