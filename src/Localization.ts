export interface Locals {
    no_data: string;
    fetch_data_fails: string;
    loading: string;
}


export const locals: Locals = {
    fetch_data_fails: 'Fail To Fetch Data',
    loading: 'Loading...',
    no_data: 'No Data'
}